using SYstem;

class Program {
    static void Main(string[] args) {
        // создаём потоки. в нашем случае их будет 6, но вы можете добавить ещё
        Thread A = new Thread();
        Thread B = new Thread();
        Thread C = new Thread();
        Thread D = new Thread();
        Thread E = new Thread();
        Thread F = new Thread();
    }
}
